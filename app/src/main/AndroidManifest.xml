<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tinhtong2so">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GetIntent"></activity>
        <activity android:name=".PutExtra" />
        <activity android:name=".ReadFileInternet" />
        <activity
            android:name=".ui.login.ReadFileInternet"
            android:label="@string/title_activity_read_file_internet" />
        <activity android:name=".AsyncTask" />
        <activity android:name=".Pheptinh2so" />
        <activity android:name=".RutbaiRandom" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>